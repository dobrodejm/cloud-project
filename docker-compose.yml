services:
  web:
    image: nginx:1.23.1
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf
  app:
    build:
      dockerfile: Dockerfile
    volumes:
      - ./app:/app